A python program that scrapes search results from google to help you say answers in online class.

## Install the requirements first:

`python -m pip install -r requirements.tct`

## Run the program:

`python answerer.py`
